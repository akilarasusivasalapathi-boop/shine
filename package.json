{
  "name": "shine-website",
  "version": "1.0.0",
  "private": true,
  "description": "SHINE Testing Services — multi-page static site with a Cloud Run contact endpoint.",
  "scripts": {
    "build": "node build.js",
    "start": "node build.js && node server.js",
    "serve": "node server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
